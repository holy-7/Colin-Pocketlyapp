# Colin记账 — 需求文档

> **方法论**：Loop Engineering（[[06-Wiki/Loop-Engineering]]）
> **版本**：v1.0 | **日期**：2026-07-20
> **设计原则**：人不在循环中——本需求文档本身就是 Loop 的"可验证目标"定义。

---

## 一、产品定义

### 1.1 一句话

**Colin记账** = 数据自控 + 极简无广告 + AI 对话式财务助手。

### 1.2 差异化（4 个核心价值点）

| 价值点 | 说明 |
|:---|:---|
| **数据完全自控** | 端到端加密，不上送第三方，数据导出权 |
| **极简无广告** | 不卖流量、不推理财、不诱导消费 |
| **AI 对话式分析** | "我这个月钱花哪了？" → AI 直接回答，不是图表 |
| **与 Wiki 联动** | 分析结论同步到 Obsidian 复盘笔记，知识闭环 |

### 1.3 用户画像（MVP-1：自用）

- 自己就是唯一用户（简化：无多租户、无注册登录）
- 日常使用场景：餐后随手记、月末看报表、超支收提醒
- 技术能力：能干 CLI 运维，但日常记账要低摩擦

---

## 二、Loop 驱动的开发计划

> 核心思想：每个阶段 = 一个 Loop。Loop 有明确的**可验证目标**、**执行 Agent**、**验证器**和**刹车机制**。

### 2.1 Loop 全景图

```
┌─────────────────────────────────────────────────────────────┐
│                   Colin记账 开发 Loop 系统                   │
│                                                             │
│  Loop 0：技术选型与架构设计（Open Loop，人在场）             │
│     ↓                                                       │
│  Loop 1：MVP-1 桌面端（Closed Loop → 可验证目标驱动）        │
│     ↓                                                       │
│  Loop 2：MVP-2 移动端+双端同步（Closed Loop）                │
│     ↓                                                       │
│  Loop 3：V1.0 AI 助手（Open→Closed 两步走）                  │
│     ↓                                                       │
│  Loop 4：V1.5 多输入方式（OCR/语音/导入）                    │
│     ↓                                                       │
│  Loop 5：V2.0 投资+家庭共享（扩展场景）                      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 Loop 设计总表

| Loop | 阶段 | 类型 | 可验证目标 | 预估时长 |
|:---|:---|:---|:---|:---|
| L0 | 技术选型与架构设计 | Open | 输出技术选型文档 + 可运行的空 Electron 项目骨架 | 1-2 天 |
| L1 | MVP-1 桌面端 | Closed | Electron 桌面应用 + 手动记账 + 基础报表 + 预算提醒，全部通过 L1 验收标准 | 2-4 周 |
| L2 | MVP-2 移动端+同步 | Closed | APP 记账 + 双端数据一致 + 离线可用 | 3-5 周 |
| L3 | V1.0 AI 助手 | Open→Closed | 对话式分析可用，回答准确率 ≥ 80% | 3-4 周 |
| L4 | V1.5 多输入 | Closed | OCR/语音/导入至少 1 种上线，识别准确率 ≥ 90% | 3-4 周 |
| L5 | V2.0 扩展场景 | Open→Closed | 投资记录 + 家庭共享可用 | 4-6 周 |

---

## 三、Loop 五大组件映射

> 按"**先建 Inspector，再建 Loop**"原则，五大组件在 Colin记账 中的落地规划。

### 3.1 Skills（技能沉淀 → 最先建）

**Colin记账 的 SKILLS.md 应包含**：

- 技术栈约定（前端框架、后端语言、数据库）
- 代码规范（TypeScript strict、命名约定、目录结构）
- 构建与测试命令
- 已知坑（环境变量、依赖版本、跨平台兼容）
- 数据模型定义（Account / Transaction / Category / Budget / Tag）

**执行规则**：每个 Loop 结束后，新学到的规则必须写入 SKILLS.md。

### 3.2 Sub-agents（分工协作）

```
┌────────────────┐
│  Planner Agent │  ← 架构决策、任务分解（模型：强）
│  (L0 担当)    │
└───────┬────────┘
        │
        ↓ 产出任务规格
┌────────────────┐
│  Writer Agent  │  ← 编码实现（模型：强）
└───────┬────────┘
        │
        ↓ 产出代码
┌────────────────┐
│ Reviewer Agent │  ← 代码审查+测试（模型：便宜）
└───────┬────────┘
        │
        ↓ 产出审查报告
┌────────────────┐
│  E2E Agent     │  ← 手动验证 + 探索性测试（人担当 MVP-1）
└────────────────┘
```

> **关键规则**：写代码的和审代码的必须是两个 Agent（或两个人）。

### 3.3 Connectors（外部连接）

| 连接 | 工具 | 用途 |
|:---|:---|:---|
| **GitHub** | MCP / gh CLI | 代码仓库、PR、CI |
| **飞书** | WorkBuddy 飞书连接器 | 开发进度通知、Loop 异常告警 |
| **LLM API** | Claude / GPT API | AI 助手能力（V1.0+） |
| **Obsidian** | 本地文件读写 | Wiki 联动（分析结论 → 复盘笔记） |

### 3.4 Worktrees（并行隔离）

```
主仓库 (main)
   ├── worktree: feature/desktop-mvp-1    → Loop 1 开发
   ├── worktree: bugfix/xxx           → 并行修 Bug
   └── worktree: experiment/ai-chat   → AI 助手原型探索
```

### 3.5 Automations（自动触发 → 最后建）

| 自动化 | 触发条件 | Loop 阶段启用 |
|:---|:---|:---|
| **每日测试** | Cron 每早 6 点 | L1 完成时 |
| **Lint 检查** | PR 创建时自动 | L1 完成时 |
| **E2E 冒烟测试** | PR 创建时自动 | L2 完成时 |
| **AI 代码审查** | PR 创建时自动 | L1 完成时 |
| **自动部署** | merge main 时 | L1 完成时 |

---

## 四、功能清单与验收标准（传感器的设计）

> **这是本需求文档最关键的部分。** Loop Engineering 的核心洞察：传感器决定收敛速度。
> 每个功能的验收标准不仅是"做完了吗"，更是"做的对吗"的**强信号反馈**。

### 4.1 数据模型

```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│  Account  │────→│  Transaction  │←────│ Category │
│  (账户)   │     │  (交易记录)   │     │  (分类)   │
└──────────┘     └──────┬───────┘     └──────────┘
                        │
                        ↓
                  ┌──────────┐     ┌──────────┐
                  │  Budget  │     │   Tag    │
                  │  (预算)   │     │  (标签)   │
                  └──────────┘     └──────────┘
```

| 实体 | 核心字段 | 验证标准 |
|:---|:---|:---|
| **Account** | id, name, type(现金/银行卡/信用卡), balance, currency | 创建/编辑/删除均可回显；余额计算正确 |
| **Transaction** | id, account_id, category_id, amount, type(收入/支出), date, note, tags[] | 输入所有字段 → 列表正确显示 → 编辑后数据一致 |
| **Category** | id, name, type(收入/支出), parent_id(多级), icon, color | 默认分类（食/行/住/娱乐/购物/医疗/教育/其他）存在；自定义分类有效 |
| **Budget** | id, category_id(可选), amount, period(月), start_date | 超预算时前端有视觉提示，不低于阈值 |
| **Tag** | id, name, color | 标签可多选、可筛选 |

### 4.2 MVP-1 功能验收矩阵（强信号标准）

| 功能 | 输入是什么 | **强信号验收标准**（弱→强） |
|:---|:---|:---|
| **记一笔** | 金额、分类、时间、备注、账户 | ① Pass/Fail：输入完能保存 ② 结构化：金额格式校验（支持小数点2位，拒绝负数以外的特殊字符） ③ **可定位反馈**：错误时提示"金额格式错误，仅支持数字和小数点，最大 99999999.99" |
| **交易列表** | 按时间倒序展示所有交易 | ① 列表正确渲染 ② 空状态显示引导文案"还没有记账，点此记第一笔" ③ **分页/滚动**：1000+ 条数据不卡顿（Performance API 确认 < 200ms） |
| **月度报表** | 本月支出总额、分类占比饼图、日趋势折线图 | ① 总数 = 列表中本月支出 sum ② 饼图各分类占比 sum = 100% ③ **可定位反馈**：点击饼图分区 → 跳转到该分类的交易列表 |
| **预算与提醒** | 设置月度预算 → 超出时提醒 | ① 预算设置成功显示 ② 进度条实时更新 ③ **可定位反馈**：超预算时顶部 banner 显示"本月已超预算 ¥XX"，超过 120% 时颜色从黄变红 |
| **分类管理** | 增删改分类 | ① 默认 8 个分类不可删除 ② 自定义分类可删 ③ **约束检查**：删除有交易的分类时 → "该分类下有 XX 笔交易，不可删除（需先迁移数据）" |
| **数据导出** | 导出 CSV/JSON | ① 导出文件可被 Excel 正确打开 ② 中文不乱码 ③ 导出后重新导入 → 数据一致（round-trip test） |

### 4.3 MVP-2 功能验收标准

| 功能 | 强信号验收标准 |
|:---|:---|
| **移动端 APP** | 与桌面端功能一致；启动时间 < 2s；在 iPhone 13/Android 中端机上无崩溃 |
| **双端同步** | 桌面端记账 → APP 3s 内可见（轮询或 WebSocket）；断网记账 → 恢复后自动同步 → 无数据丢失 |
| **离线记账** | 飞行模式下记 5 笔 → 联网后全部同步 → 时间戳一致 |
| **冲突处理** | 双端同时编辑同一笔交易 → 按"最后修改时间戳"策略合并 → 冲突时有 UI 提示 |

### 4.4 V1.0 AI 助手验收标准

| 功能 | 强信号验收标准 |
|:---|:---|
| **支出分析** | 问"这个月钱花哪了？" → 返回分类占比 + 具体金额 + 一句话总结。**验证**：AI 输出的各分类金额 sum = 实际总额（Function Calling 查数据库，LLM 不编数字） |
| **异常识别** | 自动检测 3σ 异常（某分类支出超过历史均值+3倍标准差）→ 主动推送。**验证**：手工构造异常数据 → AI 能否识别 |
| **趋势预测** | 问"按这个节奏月底会超支吗？" → 返回预测金额 + 可信度。**验证**：用历史月份的"前 15 天数据"测试 → 预测误差 < 20% |
| **对话准确性** | 20 个预设问题（含 5 个边界/歧义问题）→ AI 回答准确率 ≥ 80%。**验证**：人工评估，5 分制 ≥ 4 分 |
| **隐私安全** | ⚠️ 关键！交易金额、账户余额等敏感字段**不直接**送入 LLM（先脱敏或仅送聚合统计）。**验证**：审查 LLM API 调用日志，确认无明文金额数据 |

---

## 五、技术选型（2026-07-20 最终调整 ✅）

> ⚠️ 2026-07-20 13:53 最终调整：引入 Supabase 实现桌面端+移动端数据实时互通。
> 参考来源：[[03-Resources/../黑马记账 APP README]]（桌面端技术栈）

| 决策项 | 原方案（纯本地） | 最终方案（云端互通） | 决策理由 | 状态 |
|:---|:---|:---|:---|:---|
| **桌面外壳** | Electron | Electron（保持） | 黑马同方案 | ✅ |
| **界面框架** | React 18 + Vite + TS | React 18 + Vite + TS（保持） | AI 辅助友好 | ✅ |
| **UI 组件库** | Ant Design 5 | Ant Design 5（保持） | 黑马同方案 | ✅ |
| **图表库** | Recharts | Recharts（保持） | 饼图/折线图 | ✅ |
| **状态管理** | Zustand | Zustand（保持） | 轻量 | ✅ |
| **移动端** | 无（纯本地不同步） | React Native / Flutter（MVP-2） | 等桌面跑稳再定 | ⏳ |
| **后端/云服务** | 无 | **Supabase（BaaS）** | PostgreSQL + 实时同步 + 开源 + 免费 | ✅ |
| **数据库** | SQLite (sql.js) 本地 | **Supabase PostgreSQL（云端）+ 本地 SQLite 缓存** | 数据互通必须上云；本地缓存应对断网 | ✅ |
| **实时同步** | 无 | **Supabase Realtime（WebSocket）** | 桌面记一笔 → 手机秒级可见 | ✅ |
| **部署** | electron-builder | electron-builder（保持） | 打包 .exe/.dmg | ✅ |
| **AI 模型** | Claude API / 本地模型 | Claude API / 本地模型（保持） | V1.0 启用 | ✅ |

**数据流（最终版）**：
```
桌面 Electron  ──→ supabase-js ──→ Supabase (PostgreSQL)  ←── supabase-js ──→ 移动 APP
    │                                    │                                    │
    └── 本地 SQLite ───────── 断网时缓存 ─→ 联网后增量同步 ←── 断网时缓存 ── 本地 SQLite ──┘
```

**架构变化**：
- ❌ 删除：纯本地 sql.js、IPC 数据库通道（替换为 supabase-js）
- ✅ 新增：Supabase 项目、supabase-js 客户端、Realtime 订阅
- ✅ 新增：本地 SQLite 缓存层（离线场景降级）
- 🔄 数据流：`IPC → sql.js` → **`supabase-js → PostgreSQL + 本地缓存`**
- 🔄 同步策略：在线实时同步，离线本地缓存，联网后增量合并（冲突用"最后写入胜出"）

详见 [[Colin记账/SKILLS.md]] 中的具体版本号和构建命令。

---

## 六、Loop Engineering 风险管控

### 6.1 三大风险在本项目中的表现

| 风险 | 本项目中具体表现 | 刹车机制 |
|:---|:---|:---|
| **Token 焚烧炉** | AI 助手无限调用 LLM，账单爆炸 | ① 单次对话最多 10 轮 ② 单日 LLM 调用预算 $5 ③ 功能开关：用户可关闭 AI 助手 |
| **理解力债务** | AI 写的后端代码自己看不懂，debug 困难 | ① 每个 feature 的 PR 必须人工 review ② 关键模块写注释说明"为什么这样设计" ③ 每周抽 1 个模块向自己解释 |
| **认知投降** | 完全把设计和决策权交给 AI，自己的工程能力退化 | ① L0 技术选型必须自己做判断 ② 每个 Loop 结束写一篇设计决策记录 ③ 能向任何人解释清楚"Colin记账 的架构为什么这样" |

### 6.2 每个 Loop 的刹车机制

```
Loop 刹车三件套（硬编码到开发规范中）：
┌──────────────────────────────────┐
│ MAX_ITERATIONS = 20              │  ← 每个 Loop 最多迭代 20 个 task
│ MAX_DURATION = 锁定阶段时长 × 1.5 │  ← 超时自动进入 Review 阶段
│ NO_PROGRESS_LIMIT = 3            │  ← 连续 3 个 task 无有效 commit → 暂停
└──────────────────────────────────┘
```

---

## 七、Memory（记忆管理）

### 7.1 开发过程记忆

```
Colin记账/
├── SKILLS.md          ← Loop 技能积累（项目知识持久化）
├── LOOP-STATE.md      ← 当前 Loop 执行状态（待处理/已处理/失败任务）
├── DECISIONS.md       ← 架构决策记录（为什么选这个技术栈、为什么这样设计）
└── CHANGELOG.md       ← 版本变更日志
```

### 7.2 LOOP-STATE.md 格式

```markdown
# Loop State — [Loop 编号] [日期]

## 目标
[MVP-1: 桌面端上线，通过全部验收标准]

## 待处理
- [ ] 数据库 schema 设计
- [ ] 账户 CRUD API
- [ ] 交易 CRUD API
- [ ] 前端记账页面
- [ ] 前端报表页面

## 已完成
- [x] 项目脚手架搭建（2026-07-XX）
- [x] ...

## 失败/卡住
（放入 Triage，等待人工判断）
```

### 7.3 上下文重置策略（致敬 Ralph Loop）

每个开发 Loop 启动时：
1. 读 `SKILLS.md`（固定锚点 → 项目知识）
2. 读 `LOOP-STATE.md`（当前累积状态）
3. 读 `DECISIONS.md`（架构约束）
4. 调用 Agent 执行下一个 task
5. 更新 `LOOP-STATE.md`

> **不累积对话历史，用文件系统传递状态。**

---

## 八、MVP-1 详细功能流转

### 8.1 核心流程

```
┌──────────────────────────────────────────────────┐
│                    MVP-1 核心流程                  │
│                                                    │
│  登录（自用→跳过）                                  │
│     ↓                                              │
│  ┌─────────┐     ┌──────────┐     ┌────────────┐ │
│  │ 记一笔   │ →  │ 交易列表  │ →  │  月度报表   │ │
│  │ 选分类   │     │ 筛选/搜索 │     │  饼图+趋势  │ │
│  │ 填金额   │     │ 编辑/删除 │     │  预算进度   │ │
│  └─────────┘     └──────────┘     └────────────┘ │
│                                                    │
│  ┌─────────────────────────────────────────────┐  │
│  │              设置（分类/预算/导出）            │  │
│  └─────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

### 8.2 页面清单

| 页面 | 路由 | 核心组件 |
|:---|:---|:---|
| 首页/记账页 | `/` | 快捷记账表单（金额 Autofocus）、今日已记列表 |
| 交易列表 | `/transactions` | 筛选器（分类/日期/金额范围）、交易卡片列表 |
| 月度报表 | `/report` | 支出分类饼图、日趋势折线图、预算进度环 |
| 设置 | `/settings` | 分类管理、预算设置、数据导出 |

### 8.3 数据访问层（supabase-js，替代 IPC）

> 数据通过 supabase-js 直连 Supabase PostgreSQL。渲染进程和主进程都可调用 supabase-js。

```
npm install @supabase/supabase-js
```

| 操作 | 方式 | 说明 | 验证器 |
|:---|:---|:---|:---|
| 创建交易 | `supabase.from('transactions').insert()` | — | amount > 0, category_id 存在, date ISO 8601 |
| 查询交易列表 | `.select().filter().order()` | 支持分类/日期/金额筛选 + 分页 | — |
| 更新/删除交易 | `.update() / .delete()` | — | id 存在 |
| 分类管理 | `.from('categories').select/insert/update/delete()` | 系统预置分类不可删 | 分类下无交易时才能删 |
| 月度报表 | `.from('transactions').select()` + 聚合 | 按月 sum group by category | sum = 数据库实际 sum |
| 数据导出 | `.select()` → CSV/JSON 生成 | — | round-trip 测试 |
| **实时同步** | `.from('transactions').on('INSERT', callback).subscribe()` | WebSocket 监听表变更，双端实时一致 | 桌面创建 → APP 3s 内可见 |

**数据流**：
```
React 组件 → Zustand Store → supabase-js → Supabase (PostgreSQL)
                              ↓
                     Supabase Realtime (WebSocket)
                              ↓
                     双端 Zustand 实时更新 → React 重渲染
```

**离线降级**：
```
在线：supabase-js 直连
  ↓ 检测到 network error
离线：本地 SQLite 读写（sql.js 作为缓存层）
  ↓ 检测到 network restore
自动批量同步 → supabase.upsert() → 冲突：最后写入胜出
```

**隐私**：交易敏感数据（金额、备注）存 PostgreSQL；Row Level Security 保证只有自己能看到；可选择性对敏感字段加密。

---

## 九、非功能需求

| 维度 | 标准 | 验证方式 |
|:---|:---|:---|
| **性能** | 应用启动 < 1s，界面切换 < 200ms | Performance API |
| **可用性** | 记账流程 ≤ 3 步（打开 → 输入金额 → 确认） | 手动走查 |
| **数据安全** | 本地 SQLite 加密（sql.js），无需服务器，数据完全在用户设备上 | 安全自查清单 |
| **可维护性** | TypeScript strict mode，ESLint 0 warning，测试覆盖率 ≥ 60% | CI 检查 |
| **打包** | electron-builder 打包 Win(.exe) + Mac(.dmg)，安装包 < 200MB | 构建后手动安装测试 |
| **移动端** | MVP-2 阶段：React Native / Flutter 原生 APP | MVP-1 不涉及 |

---

## 十、参考与关联

- 灵感来源：[[00-灵感库/2026-07-19-个人记账APP.md]]
- 方法论：[[06-Wiki/Loop-Engineering]]、[[06-Wiki/Harness工程]]、[[06-Wiki/Ralph循环]]
- 技术参考：[[06-Wiki/提示词工程四层体系]]（AI 助手基于四层架构设计）

---

*本文档随开发 Loop 迭代持续更新。每个 Loop 结束后记录实际 vs 计划的偏差。*
